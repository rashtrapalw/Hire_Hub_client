# HireHub - Client

Minimal React client (Vite) for HireHub.

Install and run (PowerShell):

```powershell
cd client
npm install
npm run dev
```

Set backend API by creating `.env` and define `REACT_APP_API_URL` if not `http://localhost:5000`.
